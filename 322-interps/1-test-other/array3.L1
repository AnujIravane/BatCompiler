; array2.L1
; Allocate an array of array
; Except each array is different

(:main
  (:main
    0 0
    (rdi <- 9)
    (rsi <- 7)
    (call allocate 2)
    ((mem rax 8) <- 5)
    ((mem rax 16) <- 3)
    ((mem rax 24) <- 11)
    ((mem rax 32) <- 13)
    (rdi <- rax)
    (call print 1)
    (return)))
