(:main
 (:main
  0
  0
  (r12 <- r12)
  (r13 <- r13)
  (r14 <- r14)
  (r15 <- r15)
  (rbp <- rbp)
  (rbx <- rbx)
  (rdi <- 15)
  (rsi <- 333)
  (call allocate 2)
  (r10 <- 1)
  (r11 <- 3)
  (r8 <- 5)
  (r9 <- 7)
  (rcx <- 9)
  (rdi <- 11)
  (rdx <- 13)
  (rax <- rax)
  (rsi <- rax)
  (rax += 8)
  ((mem rax 0) <- r10)
  (rax += 8)
  ((mem rax 0) <- r11)
  (rax += 8)
  ((mem rax 0) <- r8)
  (rax += 8)
  ((mem rax 0) <- r9)
  (rax += 8)
  ((mem rax 0) <- rcx)
  (rax += 8)
  ((mem rax 0) <- rdi)
  (rax += 8)
  ((mem rax 0) <- rdx)
  (rdi <- rsi)
  (call print 1)
  (r12 <- r12)
  (r13 <- r13)
  (r14 <- r14)
  (r15 <- r15)
  (rbp <- rbp)
  (rbx <- rbx)
  (return)))
