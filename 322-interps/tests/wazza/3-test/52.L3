((:main)
 (:main ()
  (let ([a 4])
   (let ([b 1])
    (let ([arr1 (new-array a b)])
     (let ([d 2])
      (let ([e 2])
       (let ([arr2 (new-array d e)])
        (let ([f (aset arr2 1 arr1)])
         (print arr2))))))))))
