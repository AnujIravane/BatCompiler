((:main)
 (:main ()
  (let ([arr1 (new-array 3 2)])
   (let ([arr2 (new-array 4 2)])
    (let ([arr3 (:create_big_array arr1 arr2)])
     (print arr3)))))
 (:create_big_array (arr1 arr2)
  (let ([n1 (alen arr1)])
   (let ([n2 (alen arr2)])
    (let ([n3 (+ n1 n2)])
     (new-array n3 2))))))
