((:main)
 (:main ()
  (let ([arr (new-array 2 3)])
   (let ([p (print arr)])
    (let ([new_arr (:pop arr)])
     (print new_arr)))))
 (:pop (arr)
  (let ([n (alen arr)])
   (let ([n (- n 1)])
    (let ([m (aref arr 0)])
     (new-array n m))))))
