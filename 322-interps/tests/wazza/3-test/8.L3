((:main)
  (:main ()
    (let ([arr (new-array 2 3)])
      (let ([zero (aset arr 0 6)])
        (let ([swapped (:swap_pair arr)])
          (print swapped)))))
  (:swap_pair (arr)
    (let ([first (aref arr 0)])
      (let ([second (aref arr 1)])
        (let ([firstswap (aset arr 0 second)])
          (let ([secondswap (aset arr 1 first)])
            arr))))))
    
      
