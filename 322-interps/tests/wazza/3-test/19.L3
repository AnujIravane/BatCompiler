((:main)
  (:main ()
    (let ([arr (new-array 1 3)])
      (let ([firstprint (print arr)])
        (let ([res (:push arr 4)])
          (print res)))))
  (:push (arr n)
    (let ([newarr (new-array 2 3)])
      (let ([zero (aset newarr 1 n)])
        newarr))))
