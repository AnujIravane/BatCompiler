((:main)
 (:main ()
  (let ([a (new-array 5 5)])
   (let ([p (print a)])
    (let ([new_a (:pop a)])
     (print new_a)))))
 (:pop (a)
  (let ([n (alen a)])
   (let ([n (- n 1)])
    (let ([m (aref a 0)])
     (new-array n m))))))