((let ([t (new-tuple 1)])
  (let ([c (make-closure :f t)])
    (let ([p (closure-proc c)])
      (let ([x1 (p 1)])
        (let ([v (closure-vars c)])
          (print v))))))
  (:f (x)
    (print x)))