(:main
  (:main
    0 1
    (rdi <- 1)
    (r10 <- 2)
    (r11 <- 3)
    (r8 <- 4)
    (r9 <- 5)
    (rax <- 6)
    (rcx <- 7)
    (rdx <- 8)
    (rsi <- 9)
    ((mem rsp 0) <- 11)
    (rdi <- rdi)
    (rdi += r10)
    (rdi += r11)
    (rdi += r8)
    (rdi += r9)
    (rdi += rax)
    (rdi += rcx)
    (rdi += rdx)
    (rdi += rsi)
    (r10 <- (mem rsp 0))
    (cjump r10 < rdi :true :false)
    :true
    (rdi <- 1)
    (call print 1)
    (return)
    :false
    (rdi <- 3)
    (call print 1)
    (return)))