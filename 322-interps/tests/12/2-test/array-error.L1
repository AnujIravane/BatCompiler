(:main
  (:main
    0 0
    (rdx <- 1)
    (rdi <- 5)
    (rsi <- 7)
    (call allocate 2)
    (rdi <- rax)
    (rsi <- 9)
    (call array-error 2)
    (rdi <- 2)
    (rdi += rdx)
    (call print 1)
    (return)))