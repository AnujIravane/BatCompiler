((:f 4 5)
 (:f (x y)
  (let ([x1 (+ x 1)])
   (let ([y2 (+ y 2)])
    (let ([cmp (< x1 y2)])
     (let ([d1 (print 0)])
      (let ([d2 (print 1)])
       (if cmp d1 d2)
      )
     )
    )
   )
  )
 )
)
